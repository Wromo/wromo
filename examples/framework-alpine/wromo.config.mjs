import { defineConfig } from 'wromo/config';

// https://wromo.build/config
export default defineConfig({
	// No integrations are needed for AlpineJS support, just use Wromo components!
	integrations: [],
});
